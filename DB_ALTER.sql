ALTER TABLE `user_role` ADD UNIQUE( `role_name`);
#third release alter your table copy and paste your dabase sql command 
ALTER TABLE `user` ADD UNIQUE( `user_name`);

ALTER TABLE `out_source_company` ADD UNIQUE( `company_name`);
